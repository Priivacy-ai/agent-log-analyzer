version: 2

project_name: agent-analyzer

before:
  hooks:
    - go mod download

builds:
  - id: agent-analyzer
    main: ./cmd/agent-analyzer
    binary: agent-analyzer
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .Date }}

archives:
  - id: cli
    ids:
      - agent-analyzer
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - README.md
      - LICENSE
      - docs/distribution.md

checksum:
  name_template: checksums.txt
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot-{{ .ShortCommit }}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  github:
    owner: Priivacy-ai
    name: agent-log-analyzer
  draft: true
  prerelease: auto
  name_template: "{{ .ProjectName }} {{ .Tag }}"
  footer: |
    Release provenance:
    - Source: https://github.com/Priivacy-ai/agent-log-analyzer
    - Commit: {{ .Commit }}
    - Checksums: checksums.txt

brews:
  - name: agent-analyzer
    repository:
      owner: Priivacy-ai
      name: homebrew-agent-analyzer
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    homepage: https://github.com/Priivacy-ai/agent-log-analyzer
    description: Local-first agent coding log profiler that uploads sanitized reports only.
    license: Proprietary
    directory: Formula
    install: |
      bin.install "agent-analyzer"
    test: |
      system "#{bin}/agent-analyzer", "version"

scoops:
  - name: agent-analyzer
    repository:
      owner: Priivacy-ai
      name: scoop-agent-analyzer
      branch: main
      token: "{{ .Env.SCOOP_BUCKET_GITHUB_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    homepage: https://github.com/Priivacy-ai/agent-log-analyzer
    description: Local-first agent coding log profiler that uploads sanitized reports only.
    license: Proprietary
